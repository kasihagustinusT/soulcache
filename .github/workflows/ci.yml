name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions: {}

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    name: CI (Node ${{ matrix.node-version }})
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        node-version: [20, 22]
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm build

      - name: Typecheck
        run: pnpm typecheck

      - name: Lint
        run: pnpm lint

      - name: Test
        run: pnpm test

      - name: Test (bug verification suite)
        run: pnpm test:bugs

      - name: Test (React bindings)
        run: pnpm --filter @soulcache/react test

      - name: Test (integration gate)
        run: pnpm test:integration

      - name: Test (stress gate)
        run: pnpm test:stress

      - name: Test (memory gate)
        run: pnpm test:memory

      - name: Validate monorepo
        run: pnpm validate:monorepo

      - name: Validate packages
        run: node scripts/validate-exports.mjs
        if: matrix.node-version == 22

      - name: Check public API (CA-7)
        run: pnpm check:api
        if: matrix.node-version == 22

      - name: Validate docs
        run: node docs/scripts/validate.mjs
        if: matrix.node-version == 22

  coverage:
    name: Tests (coverage)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm build

      - name: Test with coverage (core + devtools-core)
        run: pnpm test:coverage

      - name: Test with coverage (React bindings)
        run: pnpm --filter @soulcache/react test:coverage

      - name: Upload coverage
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage
          path: |
            coverage/
            packages/react/coverage/
          retention-days: 7
